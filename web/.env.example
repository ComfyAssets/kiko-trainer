# API endpoint the web UI talks to
VITE_API_HOST=localhost
VITE_API_PORT=8888

# Paths used for script previews and exports in the UI
# These should reflect the backend/container filesystem, not the browser.
# Defaults point to repo-relative folders.
VITE_MODELS_DIR=models
VITE_OUTPUTS_DIR=outputs

